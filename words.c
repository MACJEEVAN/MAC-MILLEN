#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
    char s[100];
    int len,count=0,i,m;
    clrscr();
    gets(s);
    len=strlen(s);
    for(i=0;i<len;i++)
     {
        if(s[i]==' ')
        count++;   
    }
    m=count+1;
    printf("%d",w);
    getch();   
}
